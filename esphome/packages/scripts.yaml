# ============================================================
# M5Stack Tab5 脚本配置文件
# 用于存放LVGL界面的自定义脚本逻辑
# ============================================================

script:
  # ==================== 初始化脚本 ====================
  # 空脚本，用于后续添加LVGL界面逻辑
  - id: dummy_script
    then:
      - logger.log: "LVGL脚本已初始化。"
